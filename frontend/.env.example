# In dev, requests go through Vite's own proxy (see vite.config.ts) at /api,
# so VITE_API_BASE_URL can stay '/api' locally. Point it at the real API
# origin for a production build served from a different host than the API.
VITE_API_BASE_URL=/api
