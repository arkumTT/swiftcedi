# SwiftCedi Frontend

Not started yet. Per `CLAUDE.md`'s build order, Module 11 (RBAC/Audit) and
Module 7 (GL) were built first as backend shared infrastructure — see
`../backend`. The React frontend starts once Module 1 (Branch) exists and
there's a first real screen (branch setup) to build against.

Planned dashboards (per role): Owner/Executive, Branch Manager, Loan
Officer, Cashier/Teller, Field Agent, Investor-facing views.
