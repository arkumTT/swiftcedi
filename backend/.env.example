NODE_ENV=development
PORT=4000

# PostgreSQL connection for the local dev database
DATABASE_URL=postgres://swiftcedi:swiftcedi_dev@localhost:5432/swiftcedi_dev

# PostgreSQL connection used by `npm test` (kept separate from dev data)
TEST_DATABASE_URL=postgres://swiftcedi:swiftcedi_dev@localhost:5432/swiftcedi_test
